#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int num;
    long long sum = 0;

    long long total = (long long)n * (n + 1) / 2;

    for(int i = 0; i < n - 1; i++) {
        scanf("%d", &num);
        sum += num;
    }

    printf("%lld", total - sum);

    return 0;
}
